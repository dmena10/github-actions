name: Github actions - Push Template Basic
run-name: Running - ${{ github.event_name }}
on:
  push:
      branches:
            - master
            - develop
jobs:
  on-push:
    runs-on: ubuntu-latest
    steps:
        - name: Comentario
          run: |
                echo "Comentario do workflow PUSH para branch"
